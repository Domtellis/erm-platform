# Workflow state machine specs (event/command lists)

## Each workflow spec is machine-readable and includes:
- states
- commands (intent)
- events (facts)
- transitions
- validations/guards
- SoD/RBAC constraints
- audit logging requirements